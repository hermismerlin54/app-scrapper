name: aura_reader
description: Advanced Reading Engine for Android.
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.1
  html: ^0.15.4
  flutter_inappwebview: ^6.0.0
  provider: ^6.1.1
  path_provider: ^2.1.2
  sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
